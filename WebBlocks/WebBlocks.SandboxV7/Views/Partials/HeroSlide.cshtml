@inherits WebBlocks.ViewTemplates.WebBlocksTemplatePage

@{
    WebBlocks.CssClasses.Add("hero-slide");
    
    var imageUrl = Umbraco.Media(@CurrentBlock.image).Url;
    var imageResized = imageUrl + "?width=375&height=150&mode=crop";

    <img alt="Slide" src="@imageResized" />
}
